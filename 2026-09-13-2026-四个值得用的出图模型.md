# 出图总是糊？ComfyUI 默认模型太老，换这四个之一（2026）

> 来源：落木繁叶.W（微信公众号）
> 原文：https://mp.weixin.qq.com/s/k05-JP5PPTPzLAFZ7HdoCw
> 收藏时间：2026-09-13

## 现象

刚跑通第一张图就糊——细节发软、人脸像隔着毛玻璃。不怪提示词，**ComfyUI 默认挂的是 SD 1.5，2022 年的老模型**，再会调参也救不回来。

## 2026 年值得用的四个模型

| 模型 | 一句话定位 |
|------|-----------|
| **Z-Image Turbo** | 8GB 显存快速迭代首选 |
| **FLUX.1 dev** | 追求写实、图只自己玩 |
| **SDXL** | 要特定画风、要用角色 LoRA |
| **Qwen-Image** | 图里要清楚的中文字 |

**商用**：Z-Image Turbo、SDXL、Qwen-Image 都行，**避开 FLUX dev**。

## 小白第一个该下的：Z-Image Turbo

阿里通义 2025 年 11 月发布，6B 参数。四个理由：

1. **快**：4090 出一张 1024 只要 2–3 秒（FLUX 满精度 15–30 秒），改一次提示词几秒见结果，学习效率完全不同
2. **省**：FP8 版 8GB、GGUF 版 6GB，3060 这种卡也能跑
3. **能商用**：Apache 2.0，不用额外授权——实际工作里比多几个点画质值钱
4. **中英文字都能写**：老模型的老大难

## 三步装 Z-Image Turbo

**第一步，下三个文件放三个文件夹**（放错位置界面上就找不到）：

| 文件 | 目录 |
|------|------|
| `z_image_turbo_bf16.safetensors`（模型本体） | `models/diffusion_models/` |
| `qwen_3_4b.safetensors`（文本编码器） | `models/text_encoders/` |
| `ae.safetensors`（VAE） | `models/vae/` |

**第二步**：Workflow → 浏览模板 → Image 分类 → 找 Z-Image Turbo。官方模板节点已连好，不用自己搭。

**第三步**：三个加载器分别指向放好的文件 → Run。

### 显存不够怎么办

- < 14GB（如 6G 的 3060）：BF16 换 **FP8 版（约 8GB）**，或 **GGUF 量化版（约 6GB）**
- GGUF 需先在 ComfyUI Manager 里装 ComfyUI-GGUF 节点，再用它的加载器替换标准扩散模型加载器

**为什么舍得降精度**：模型塞不进显存时，ComfyUI 会把装不下的部分丢到内存，靠 PCIe 来回搬——通道速度只有显存带宽的 **1/30**。量化的价值不是"更好"，是"能装得下"；完整放进显存的量化模型，比满精度来回倒腾的快得多。

## 要点速记

- 出图糊先怀疑模型，ComfyUI 默认 SD 1.5 太老
- 四选一对号入座：Z-Image Turbo / FLUX.1 dev / SDXL / Qwen-Image
- 商用避开 FLUX dev；要中文字选 Qwen-Image；LoRA 选 SDXL
- 低显存走量化（FP8/GGUF），核心是塞进显存，不是画质
- Z-Image Turbo：Apache 2.0 + 中英写图 + 2–3 秒/张（4090）
